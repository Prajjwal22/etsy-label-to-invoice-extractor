services:
  - type: web
    name: etsy-invoice-generator
    env: python
    plan: free
    buildCommand: python -m pip install -r backend/requirements.txt && npm install && npm run build
    startCommand: python -m uvicorn backend.app:app --host 0.0.0.0 --port $PORT
